interface:
  display_name: "Marketing Harness"
  short_description: "Plan, render, review, and record accepted brand-locked marketing assets."
  default_prompt: "Use $marketing-harness to run the brand-locked marketing asset lifecycle."

policy:
  allow_implicit_invocation: false
